Implemented the requested migration artifacts for `data_generator` under:

- `migrated_azure_fn/data_generator/`
- `migrated_azure_fn/infrastructure/data_generator/main.bicep`

Included:
- unit tests first
- Azure Functions implementation
- required function app files
- Bicep template with Function App, Storage, Cosmos DB, Service Bus, Key Vault, App Insights

I did not run tests.