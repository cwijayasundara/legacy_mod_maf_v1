# Codex Harness — Dependencies
# Install: pip install -r requirements.txt

# REST API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# HTTP client (ADO REST API calls)
httpx>=0.27.0

# Azure
azure-identity>=1.16.0
azure-storage-blob>=12.19.0          # State persistence to Blob Storage

# Note: Codex CLI is installed separately via npm:
#   npm install -g @openai/codex

# ── Test dependencies ──
# pip install pytest pytest-cov pytest-asyncio httpx
# pytest
# pytest-cov
# pytest-asyncio
# httpx  (already listed above as runtime dep)
