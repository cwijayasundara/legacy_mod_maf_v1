# Learned Rules — Monotonic Knowledge Base
#
# This file grows monotonically. Rules are NEVER deleted.
# New rules are extracted when the same error appears 2+ times across modules.
# ALL agents read this file before starting any task.
#
# Format per rule:
# ## Rule N: <title>
# Pattern: <what triggers this>
# Anti-pattern: <code that causes the error>
# Better: <code that avoids the error>
# Applied in: <which agents / contexts>
# Learned from: <module(s) where this was discovered>
# Date: <YYYY-MM-DD>

# ─── Rules will be added here as migrations proceed ───
