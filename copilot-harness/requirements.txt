# Copilot Harness — Dependencies
# Install: pip install -r requirements.txt

# GitHub Copilot SDK (for custom agent logic)
github-copilot-sdk>=0.2.0

# REST API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# HTTP client (ADO REST API)
httpx>=0.27.0

# Azure
azure-identity>=1.16.0
azure-storage-blob>=12.19.0

# Configuration
pyyaml>=6.0.0

# Note: Copilot CLI is installed separately via npm:
#   npm install -g @githubnext/copilot-cli
# Or via GitHub: gh extension install github/gh-copilot

# ── Test dependencies ──
# pip install pytest pytest-cov pytest-asyncio httpx
